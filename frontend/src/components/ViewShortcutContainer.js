import React from 'react';

// rewrite this to actually fetch stored shortcuts

const ViewShortcutContainer = ({}) => {
  return <div>view shorcuts here</div>;
};

export default ViewShortcutContainer;
